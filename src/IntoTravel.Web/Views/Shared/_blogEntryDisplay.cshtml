@model IntoTravel.Web.Models.BlogEntrytDisplay

<article>
    <div class="container">
        <div class="row">
            <div class="col-lg-8 offset-lg-2 col-md-10 offset-md-1">
                <h2>@Model.Title</h2>
         <span>@Model.BlogPublishDateTimeUtc.ToString() </span>
                <br />
                @Html.Raw(Model.Content)
            </div>
        </div>
    </div>
</article>
<hr />
